<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Trinca.Soccer.App.Views.NewMatchPage"
             Title="{Binding Title}">
    <ScrollView>
        <StackLayout Orientation="Vertical"
                     Padding="10, 10"
                     HorizontalOptions="FillAndExpand"
                     VerticalOptions="FillAndExpand">
            <Label Text="Place"></Label>
            <Entry Text="{Binding Place}"></Entry>
            <Label Text="Date"></Label>
            <DatePicker Date="{Binding Date}"></DatePicker>
            <Label Text="Hour"></Label>
            <DatePicker Date="{Binding Hour}"></DatePicker>
            <Label Text="Minimum players required"></Label>
            <Entry Keyboard="Numeric" Text="{Binding MinimumPlayers}"></Entry>
            <Label Text="Match value"></Label>
            <Entry Keyboard="Numeric" Text="{Binding Value}"></Entry>
            <Label Text="With Barbecue?"></Label>
            <Switch IsToggled="{Binding WithBarbecue}"></Switch>
            <Label Text="Match value"></Label>
            <Entry Keyboard="Numeric" Text="{Binding BarbecueValue}"></Entry>
            <Button Text="Create"
                    IsVisible="{Binding ShowLoading, Converter={StaticResource NegateBooleanConverter}}"></Button>
            <ActivityIndicator IsVisible="{Binding ShowLoading}" IsRunning="{Binding ShowLoading}" Color="#000000" />
        </StackLayout>
    </ScrollView>
</ContentPage>